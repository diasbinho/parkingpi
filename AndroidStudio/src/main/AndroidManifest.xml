<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.uam.cco.mob.app.parkingpi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.InsertVacancy" />
        <activity android:name=".Activity.ViewParking_Admin" />
        <activity android:name=".Activity.InsertParking" />
        <activity android:name=".Activity.InsertUser" />
        <activity android:name=".Activity.MenuAdmin" />
        <activity android:name=".Activity.MenuUser" />
        <activity android:name=".Activity.ViewParking_User" />
        <activity android:name=".Activity.EditUserPass"></activity>
    </application>

</manifest>